# Farming Application

❤ Built a server manually using `http.createServer` modern technologies: Node.js and different modules - `http`, `url`, `fs`

###### Details

`1.0.0` - means major.minor.patch - versions

`"slugify": "^1.0.0"` - ^ means accepts minor and major updates

`"slugify": "~1.0.0"` - ~ means accepts only patch updates

`"slugify": "*1.0.0"` - \* all updates
